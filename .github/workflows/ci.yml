name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

env:
  DOTNET_VERSION: "8.0.x"
  SOLUTION_PATH: "winui3/SquirrelNotifier.WinUI3.sln"

jobs:
  build-and-test:
    runs-on: windows-2025

    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Install Windows SDK
        run: |
          # Check available SDKs
          Write-Host "Checking for installed Windows SDKs..."
          if (Test-Path "C:\Program Files (x86)\Windows Kits\10") {
            Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\Include" | Select-Object Name
          }
          # Install Windows SDK 10.1 which includes 10.0.22621.0
          choco install windows-sdk-10.1 -y --no-progress --force

      - name: Install dependencies
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      - name: Build solution
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore /p:Platform=x64 /p:IncludeWindowsSdkBuildTools=false

      - name: Verify MSI MajorUpgrade
        shell: pwsh
        run: |
          $payloadDir = (New-Item -ItemType Directory -Path "./installer-test-payload" -Force).FullName
          New-Item -ItemType File -Path (Join-Path $payloadDir "SquirrelNotifier.WinUI3.exe") -Force | Out-Null
          $outputDir = (New-Item -ItemType Directory -Path "./installer-test-output" -Force).FullName
          dotnet build winui3/SquirrelNotifier.Installer/SquirrelNotifier.Installer.wixproj `
            --configuration Release `
            /p:Platform=x64 `
            /p:HarvestPath="$payloadDir" `
            /p:PackageVersion=1.2.3 `
            /p:OutputPath="$outputDir"
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          $msi = Get-ChildItem -Path $outputDir -Filter "*.msi" -Recurse | Select-Object -First 1
          if (-not $msi) { throw "検証用 MSI が見つかりません: $outputDir" }
          ./scripts/test-msi-major-upgrade.ps1 -MsiPath $msi.FullName

      - name: Run tests with coverage
        run: |
          foreach ($type in @("line", "branch", "method")) {
            Write-Host "Running tests with coverage (type: $type)..."
            dotnet test ${{ env.SOLUTION_PATH }} `
              --configuration Release `
              --no-build `
              --verbosity normal `
              --collect:"XPlat Code Coverage" `
              --results-directory ./coverage `
              /p:CollectCoverage=true `
              /p:CoverletOutputFormat=opencover `
              "/p:ThresholdType=$type" `
              /p:Threshold=80

            if ($LASTEXITCODE -ne 0) {
              exit $LASTEXITCODE
            }
          }

      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7.0.0
        with:
          directory: ./coverage
          fail_ci_if_error: true
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: unittests
          name: codecov-umbrella

      - name: Code Analysis
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore /p:RunCodeAnalysis=true /p:TreatWarningsAsErrors=true /p:Platform=x64 /p:IncludeWindowsSdkBuildTools=false

  lint:
    runs-on: windows-2025

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Install dotnet format
        run: dotnet tool install -g dotnet-format

      - name: Run dotnet format
        run: dotnet format ${{ env.SOLUTION_PATH }} --verify-no-changes --verbosity diagnostic
        continue-on-error: false

  security-scan:
    runs-on: windows-2025
    permissions:
      security-events: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Install Windows SDK
        run: |
          # Check available SDKs
          Write-Host "Checking for installed Windows SDKs..."
          if (Test-Path "C:\Program Files (x86)\Windows Kits\10") {
            Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\Include" | Select-Object Name
          }
          # Install Windows SDK 10.1 which includes 10.0.22621.0
          choco install windows-sdk-10.1 -y --no-progress --force

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: csharp

      - name: Restore for CodeQL
        run: dotnet restore ${{ env.SOLUTION_PATH }}

      - name: Prepare WinRT generator output folders
        run: |
          $base = Join-Path "winui3/SquirrelNotifier.WinUI3" "obj/x64/Release/net10.0-windows10.0.26100.0/generated/WinRT.SourceGenerator/Generator.WinRTAotSourceGenerator"
          New-Item -ItemType Directory -Force -Path $base | Out-Null

      - name: Build for CodeQL
        run: dotnet build ${{ env.SOLUTION_PATH }} --configuration Release --no-restore /p:Platform=x64 /p:IncludeWindowsSdkBuildTools=false

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
